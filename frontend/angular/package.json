{
  "name": "blended-workflow-angular-app",
  "private": true,
  "version": "1.0.0",
  "devDependencies": {
    "http-server": "^0.9.0"
  },
  "scripts": {
    "prestart": "npm install",
    "start": "http-server . -a localhost -p 8081 -c-1"
  }
}
